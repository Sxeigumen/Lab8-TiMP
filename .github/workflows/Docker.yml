on:
  push:
    branches:
          main
          
jobs:
  Docker:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - name: PyFilePic
          run: |
            sudo docker build -t picture .
            sudo docker run picture
          
          
